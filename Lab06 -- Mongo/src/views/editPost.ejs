<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Editar Post</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" rel="stylesheet">
</head>
<body class="container" style="margin-top: 30px;">
  <h3 class="teal-text">Editar Post</h3>

  <form action="/posts/update/<%= post._id %>" method="POST">
    <div class="input-field">
      <input type="text" name="title" value="<%= post.title %>" required minlength="5" maxlength="30">
      <label class="active">Título</label>
    </div>

    <div class="input-field">
      <textarea name="content" class="materialize-textarea" required minlength="10"><%= post.content %></textarea>
      <label class="active">Contenido</label>
    </div>

    <div class="input-field">
      <input type="text" name="hashtags" value="<%= post.hashtags ? post.hashtags.join(',') : '' %>">
    </div>

    <div class="input-field">
      <input type="text" name="imageUrl" value="<%= post.imageUrl %>">
    </div>

    <button type="submit" class="btn waves-effect waves-light teal">Actualizar</button>
  </form>
</body>
</html>
